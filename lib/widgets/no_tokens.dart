import 'package:flutter/material.dart';

class NoTokens extends StatelessWidget {
  const NoTokens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}